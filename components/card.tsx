import Image from 'next/image'; import Link from 'next/link'; export function Card({p}:{p:any}){return <article><div className="relative aspect-[4/5] overflow-hidden bg-white"><Image src={p.image} alt={p.name} fill className="object-cover" sizes="25vw"/></div><div className="pt-4 flex justify-between"><div><Link href={'/products/'+p.slug} className="font-medium hover:underline">{p.name}</Link><p className="text-sm text-black/50 mt-1">{p.category}</p></div><span>${p.price}</span></div></article>}
