import {notFound} from 'next/navigation'; import Image from 'next/image'; import {getProduct} from '@/lib/products'; export default function Product({params}:{params:{slug:string}}){const p=getProduct(params.slug);if(!p)return notFound();return <main className="container py-14 grid md:grid-cols-2 gap-12"><div className="relative aspect-square"><Image src={p.image} alt={p.name} fill className="object-cover"/></div><div><p className="text-accent uppercase tracking-[.25em] text-xs">{p.category}</p><h1 className="mt-4 text-5xl">{p.name}</h1><p className="mt-6 text-xl">${p.price}</p><p className="mt-6 text-black/60 leading-7">{p.description}</p><button className="mt-8 bg-ink text-white px-7 py-3">Add to bag</button></div></main>}
